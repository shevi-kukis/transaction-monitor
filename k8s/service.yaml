apiVersion: v1
kind: Service
metadata:
  name: financial-monitor-service
spec:
  selector:
    app: financial-monitor
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer 